import {createRequire} from 'node:module';import express from 'express';import {promises as fs} from 'node:fs';import path from 'node:path';import os from 'node:os';import assert from 'node:assert/strict';import {libraryRouter,createLibrary} from '../server/library.mjs';
const file=process.argv[2];if(!file)throw new Error('Provide phantom ZIP path');const require=createRequire(import.meta.url),{chromium}=require(process.env.IVCS_PLAYWRIGHT_MODULE || 'playwright');const root=await fs.mkdtemp(path.join(os.tmpdir(),'ivcs-phantom-'));const app=express();app.use('/api/library',libraryRouter(root));app.use(express.static(path.resolve('dist')));app.get('*',(_req,res)=>res.sendFile(path.resolve('dist/index.html')));const server=app.listen(0,'127.0.0.1');await new Promise(r=>server.on('listening',r));const browser=await chromium.launch({headless:true,channel:'msedge'});
try{const page=await browser.newPage();const errors=[];page.on('pageerror',e=>errors.push(e.message));await page.goto('http://127.0.0.1:'+server.address().port);await page.getByRole('button',{name:'Save',exact:true}).waitFor();await page.locator('input[accept=".dcm,.zip"]').setInputFiles(file);const dialog=page.getByRole('dialog');await dialog.waitFor();assert.equal(await dialog.getByRole('checkbox').count(),14);await dialog.getByRole('checkbox').nth(0).check();await dialog.getByRole('checkbox').nth(1).check();await dialog.getByRole('button',{name:'Load selected series'}).click();await dialog.waitFor({state:'hidden'});await page.waitForTimeout(2000);const library=createLibrary(root),index=await library.list();assert.equal(index.patients.length,1);assert.equal(index.patients[0].studies.length,2);for(const s of index.patients[0].studies){assert.equal(s.originals,3);assert.equal(s.slices,3);assert.ok(s.acquisitionKey);const opened=await library.open(s.key);assert.ok(opened.selected.slices.every(p=>p.sourceRaw));}assert.deepEqual(errors,[]);console.log('PASS supplied phantom ZIP: 14 choices; selected acquisitions stored separately with 3 original raw DICOM each');}finally{await browser.close();server.close();}
